package de.tnttastisch.jsonlib.json.io;

public enum JsonScope {

    EMPTY_ARRAY,
    EMPTY_OBJECT,
    EMPTY_READER,
    FILLED_ARRAY,
    FILLED_OBJECT,
    FILLED_READER,
    PENDING_NAME,
    CLOSED;

}
